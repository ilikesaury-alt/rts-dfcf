# 规则变更日志

> 配套 `docs/rule-baseline-2026-09-11.md`。
>
> **冻结已于 2026-09-11 12:20 解除**，本文档从「冻结破坏记录」改为**变更日志**：
> 登记是**建议**，不是强制；目的是给未来的自己留证据，便于日后回溯
> 「这个数字为什么是现在这样」。
>
> 格式：`日期 | 文件 | 改动摘要 | 是否影响决策输出 | 值变更（前 → 后）`

---

## 冻结期记录（2026-09-11 11:40 ~ 12:20）

冻结仅持续约 40 分钟（不足 1 个交易日），未积累有效样本。
期间只发生了一组等价重构（无行为变化）：

| 日期 | 文件 | 改动摘要 | 影响输出 | 值变更 |
|---|---|---|---|---|
| 2026-09-11 | （基线）| 冻结生效，签署快照 | — | — |
| 2026-09-11 | `scanner/config.py` | 标签字面量单源收敛：新增 5 个 `TACTICS_TAG_*` 常量 + `TACTICS_SELL_TAGS` frozenset。**仅新增，未修改/删除任何既有常量**（已用脚本逐常量 diff 证明） | ❌ 无 | 无（0 改 0 删，新增 5+1） |
| 2026-09-11 | `scanner/intraday_tactics.py` | 产生端 7 处字面量 → 引用 config 常量 | ❌ 无（值逐字节相同） | 无 |
| 2026-09-11 | `scanner/display.py` | 消费端删 2 处局部 `_SELL_TAGS` 定义 → 引用 `config.TACTICS_SELL_TAGS` | ❌ 无 | 无 |
| 2026-09-11 | `scanner/final_pick.py` | 消费端 1 处字面量拷贝 → 引用 `config.TACTICS_SELL_TAGS` | ❌ 无 | 无 |

该组改动性质（判定为等价重构）：

1. **新旧标签集合逐字节一致**（`⬇减仓`/`⬇减半`/`🔻勿接`/`💰落袋`，codepoint 级验证）；
   `⬆加仓` 仍正确排除在卖出集合外。
2. **未触碰任何数值阈值**——`git show HEAD:scanner/config.py` 与工作区全常量逐项 diff：
   0 个被修改、0 个被删除、仅 5 个新增（纯字符串常量）。
3. **不改变决策输出**——`TACTICS_SELL_TAGS` 的值集合与原先 3 处硬编码完全相同，
   「命中即剔除」的判定结果逐票不变。

---

## 解冻后的变更记录

| 日期 | 文件 | 改动摘要 | 影响输出 | 值变更 |
|---|---|---|---|---|
| 2026-09-11 | （解除冻结）| 用户决定终止冻结，规则恢复自由改动。理由：确有需立即修正之处，等待 20 个交易日的机会成本更高 | — | — |

> **当时的待办（解冻后已可处理）**：
> 1. `intraday_tactics.py` 的 25 个 `TACTICS_*` 阈值常量无样本量/命中率依据
>    （见 `.workbuddy-ai/memory/2026-09-11.md`）；
> 2. 该模块规则 1 的 3% 门槛与 09-04 测评「4-6% 甜蜜带」结论冲突；
> 3. 重构建议 `docs/refactor-recommendation-2026-09-11.md` 的 P0（主表降级 +
>    0–3 只决策输出）、P3（decision 可观测性）、P3.5（候选池来源打标）等均不再受阻。


