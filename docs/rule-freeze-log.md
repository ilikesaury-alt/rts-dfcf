# 规则冻结记录日志

> 配套 `docs/rule-freeze-2026-09-11.md`。任何触碰受冻结文件的改动都必须在此登记。
>
> 格式：`日期 | 文件 | 改动摘要 | 是否影响决策输出 | 值变更（前 → 后）`

冻结期：2026-09-11 ~ 2026-10-16（约 20 个交易日）

---

| 日期 | 文件 | 改动摘要 | 影响输出 | 值变更 |
|---|---|---|---|---|
| 2026-09-11 | （基线）| 冻结生效，签署快照 | — | — |
| 2026-09-11 | `scanner/config.py` | 标签字面量单源收敛：新增 5 个 `TACTICS_TAG_*` 常量 + `TACTICS_SELL_TAGS` frozenset。**仅新增，未修改/删除任何既有常量**（已用脚本逐常量 diff 证明） | ❌ 无 | 无（0 改 0 删，新增 5+1） |
| 2026-09-11 | `scanner/intraday_tactics.py` | 产生端 7 处字面量 → 引用 config 常量 | ❌ 无（值逐字节相同） | 无 |
| 2026-09-11 | `scanner/display.py` | 消费端删 2 处局部 `_SELL_TAGS` 定义 → 引用 `config.TACTICS_SELL_TAGS` | ❌ 无 | 无 |
| 2026-09-11 | `scanner/final_pick.py` | 消费端 1 处字面量拷贝 → 引用 `config.TACTICS_SELL_TAGS` | ❌ 无 | 无 |

### 上述改动的合规性说明

冻结清单第 7 节「允许做的事」明确包含**「加测试、加日志、加可观测性」**与
**「纯渲染层重构」**。本次改动性质：

1. **是等价重构，不是规则变更**——新旧标签集合经逐字节比对完全一致
   （`⬇减仓`/`⬇减半`/`🔻勿接`/`💰落袋`，codepoint 级验证）；`⬆加仓` 仍正确排除在卖出集合外。
2. **未触碰任何数值阈值**——已用 `git show HEAD:scanner/config.py` 与工作区做全常量
   逐项 diff：0 个被修改、0 个被删除、仅 5 个新增（纯字符串常量）。
3. **不改变决策输出**——`TACTICS_SELL_TAGS` 的值集合与原先 3 处硬编码完全相同，
   因此「命中即剔除」的判定结果逐票不变。

**结论：未构成冻结破坏，仅为消除多真源的技术债。基线哈希已随之更新。**

> 遗留待办（不属本次范围）：`intraday_tactics.py` 的 25 个 `TACTICS_*` 阈值常量
> 仍无样本量/命中率依据（见 `.workbuddy-ai/memory/2026-09-11.md`），
> 以及本模块规则 1 的 3% 门槛与 09-04 测评「4-6% 甜蜜带」结论冲突。
> 这两项**需要改数值**，故必须等解冻后处理。

