# 2026-07-28

## 风险标签收敛（超买 / 主力出货 全民告警修复）

用户反馈：推荐票几乎都带"超买""主力出货"标签，导致完全不敢买入。

根因：
- `超买` 仅打在 short_term / momentum 上（这两类本就是买"正在涨"的票），旧阈值 BOLL%>1.0 / KDJ J>105 在强势主升浪中近乎必中（健康强趋势 J 常 90~115，单日大涨即破上轨）→ 标签沦为废话。
- `主力出货` Rule 2（高位高换手+超买）仅要求换手率>5%（活跃常态）+ 宽松超买，把任何活跃强势股误判为出货。

改动（config.py + enhancer.py）：
- `ST_OVERBOUGHT_BOLL` 1.0 → 1.10；`ST_OVERBOUGHT_KDJ` 105 → 115；20日涨幅>60% 维持。仅"极端超买"触发（validator 否决与 enhancer 标签共用，short_term 否决门槛同步变合理，不再误杀主升浪）。
- `主力出货` Rule 1 量比 2.0 → 2.5；Rule 2 改为需 genuine 过热换手（turnover_bonus<0，即换手>20%）而非仅>5%；Rule 3 累计涨幅下限 10 → 15。Rule 4 不变。
- 新增测试 `TestRiskFlagTightening`（test_enhancer.py）锁定：正常强势动量票不误贴标签；Rule 2 仅过热换手才触发。

验证：pytest 全量 412 passed。同步更新 AGENTS.md / STRATEGY.md 文档。

注意：`超买` 收紧使 short_term 超买否决变宽松（更多票通过），与"敢买"诉求一致；极端超买仍会否决/标记。

## Code Review（同日下午）
对风险标签收敛改动做复审：
- F1 修复：初版误留死常量 `DISTRIBUTION_TURNOVER_OVERHEAT`（未被引用），已删除，注释改为说明 `turnover_bonus<0 ⟺ 换手>20%`。
- F2 已知权衡：validator 否决与 enhancer 标签共用 `_is_overbought`，阈值收紧使 short_term 否决同步放宽（方向符合"敢买"；如需"否决严/标签宽"解耦，可加一档 tag-only 阈值）。
- F3 复核其余 5 个风险标签（涨幅过大/量价背离/趋势破位/疲劳/弱市）均有硬门控，非本次噪声源，未改。
- F4/F5 低优先观察：量价背离 pullback 分支偏宽松；主力出货当前"任一规则命中即标"，必要时可改 ≥2 独立信号。均暂不改。
- 复审后 pytest 仍 412 passed。

## 历史推荐跟踪：列表过长收敛

用户反馈：终端"历史推荐跟踪"板块条目太多、噪声大。

根因：tracker 的"观察中"门槛仅 `TRACK_STATUS_WATCH=2` 信号；而 6 个买点信号里 MA20支撑/未破位/BOLL中轨 三者本质都是"价格贴近均线"，横盘票一次凑齐 2 个即进观察列表。叠加 display 默认显示前 15 条，板块动辄十几行。

改动：
- config.py：`TRACK_STATUS_WATCH` 2 → 3（"观察中"需更实质的回调结构）；新增 `TRACK_DISPLAY_BUY_MAX=10`、`TRACK_DISPLAY_WATCH_MAX=5`。
- display.py：跟踪区块拆为两段——高确信"到买点"优先显示（封顶 10）；仅当到买点不足 10 时才用"观察中"补充（封顶 5）；到买点满额时"观察中"整段省略并提示数量。从"前 15 条混排"改为"买点优先+观察补充封顶"。
- 测试：test_tracker.py 新增 `test_status_classification_thresholds` 锁定 count==3→观察中 / count==2→过滤；`test_evaluate_perfect_buy_point` 重构为真实回调买点场景（0.5%/日浅回调 + 仅末 2 日缩量 → 缩量/未破位/BOLL中轨 共 3 信号 → 观察中）；`test_status_threshold_buy` 内部预期同步 2→3。

验证：pytest 全量 413 passed（+1 回归）。

## 风险标签硬排除（推荐了又警告矛盾的修复）

用户提问：既然能判定"主力出货""超买"等标签，为何还显示在推荐中？——指出 risk_flags 此前纯展示、无消费点（唯一例外是超买在 short_term 的条件性否决），导致"推荐了又警告危险"的矛盾。

方案（用户确认仅留 主力出货+趋势破位）：把"卖出/止损"级标签从纯展示升级为硬排除，命中即直接从所有推荐列表移除。

改动：
- config.py：新增 `RISK_FLAGS_HARD_FILTER = {"主力出货","趋势破位"}`（量价背离按用户要求不纳入，留作展示警告）。
- orchestrator.py：列表装配阶段（候选池掉榜/排名历史更新之后、各分类列表重建之前）插入过滤；新增模块级 `_candidate_excluded_by_risk(c)` 纯函数；命中时 print `[风险过滤] N 只…` 透明日志。display/feishu 不动（下游自动收到干净列表）。
- 不动：超买（仅 short_term 条件性否决）、涨幅过大/疲劳/弱市/量价背离（展示警告）；validator/enhancer/tracker/backtest 不改动。
- 测试：test_orchestrator.py 新增 `TestRiskFlagHardFilter`（8 例）锁定 主力出货/趋势破位 排除、超买/弱市/量价背离/多警告组合/无标签 均保留。
- 文档：AGENTS.md / STRATEGY.md 标注两标签为 HARD FILTER，新增硬排除说明段。

验证：pytest 全量 421 passed（413 + 8 新）。端到端模拟确认 A(主力出货)/B(趋势破位) 被排除，C(超买)/D(量价背离)/E(无) 保留。

## 历史推荐跟踪：只显示"到买点"

用户要求：历史推荐跟踪板块只显示"到买点"的票，不再显示"观察中"补充尾部。

改动：
- config.py：`TRACK_DISPLAY_WATCH_MAX` 5 → 0（"观察中"默认不显示）；注释说明需要时可改回 >0 恢复。
- display.py：`tracked` 渲染块改为"仅展示到买点"；当 `TRACK_DISPLAY_WATCH_MAX <= 0` 时，完全不渲染"观察中"段（含"另有 N 只观察中"提示行）。到买点状态分类逻辑（tracker.py，TRACK_STATUS_WATCH=3/TRACK_STATUS_BUY=4）保持不变——观察中票仍被 tracker 算出，只是终端/飞书不展示。
- 测试：新建 tests/test_display.py，2 例锁定 watch_max=0 时观察中不出、watch_max>0 时出现（monkeypatch + capsys）。

验证：pytest 全量 423 passed（421 + 2 新）。

注：tracker 的"观察中"状态判定保留（不删），仅隐藏显示；若日后想看可改 `TRACK_DISPLAY_WATCH_MAX`。

## 筛选策略过度设计评估（架构审查）

用户要求从复杂度/需求匹配/可维护性/性能四维度评估 scanner 筛选策略是否过度设计。结论：存在明显过度设计，但核心思路（多策略+交叉验证+风险硬过滤）合理，应"合并同类项"而非重写。

主要问题（证据）：
- 指标重复计算：analyzer 算 RSI/KDJ/MACD/MA/BOLL/ATR/OBV，validator 又重算（validator.py:_nf_convergence 74-80、_rb_oversold 364-386、_mo_divergence 217-222 额外算 closes[:-1]/[:-2]）。
- dims dict 跨文件契约：analyzer 写 key、validator 读 key（_rb_pattern 412-425、_try_candidate 199-202），改名即静默失效、无测试捕获。
- 5× 无条件打分：_score_stock 对每只股票调全 5 个 analyze_*（analysis.py:304-308），最终只进 1 桶 → ≥4/5 计算浪费。
- 近重复函数：banded scorer（_score_today_pct/_score_pullback_*）几乎相同；patterns.py 5 函数，engulfing×3/hammer×3/3bull×3。
- config.py 233 常量 + 注释塞满"P0-1/P1-2/2026-07-28"变更史，成 changelog。
- 一次性补丁分支（300534 案例 overbought 分支、_cap_short_term_by_sector P0-69、已删除的 VOL_RANK_HIGH_ACCUM_OVERLAP）。
- _is_overbought 别名 _st/_mo_is_overbought，两套名一个函数，反增混乱。

建议简化核心：extract_features 一次 → 3 个 profile 纯函数打分 → 同 features 上 pos_dims≥k 门禁 → 单阶段 bonus+风险硬过滤。保留 GEM 过滤/特征抽取/交叉验证/风险硬过滤/RPS tie-breaker。应向用户确认是否落地重构。

## 筛选策略去重重构（落地执行）

用户确认"执行"。以 pytest 为安全网（基线 235 核心 / 423 全量全绿），按"合并同类项"方向落地，未改任何策略语义：

- 新增 `scanner/features.py::build_features(closes, highs, lows, volumes)`：一次性算 RSI6/14、MACD、BOLL、KDJ、ADX、ATR、OBV、EMA/MA20 全部指标，供 analysis 与 validator 共用。
- `analysis.py`：5 个 `analyze_*` 加 `features=None` 入参，内部 `_compute_*_indicators`/`_ma_bull_score` 等改用 feats；仅保留 fallback 分支（feats 为 None 时自算，供单测与隔离调用）。删除不再直接使用的 compute_* 导入。
- `validator.py`：各 `validate_*` 加 `feats=None`，`_nf_convergence`/`_mo_ma_alignment`/`_mo_divergence`/`_pb_ma_trend`/`_pb_bollinger_touch`/`_rb_oversold` 改用 feats；删除重复 compute_macd 导入。
- `patterns.py`：抽 4 个共享原语（_bullish_engulfing/_hammer/_three_bullish/_breakout_3d），engulfing×3/hammer×3/3bull×3/breakout×2 重复实现收敛为"原语+各策略打分键"，5 个 detect_* 公开函数返回值完全等价。
- `analysis.py`：新增 `_band_score`（升序阶梯），收口 `_score_today_pct` 与 `_score_pullback_accumulated`；`_score_pullback_today_pct`（降序）与 `_score_pullback_volume`（含中性档）语义特殊，保持显式更清晰，不强行统一。
- `orchestrator.py`：`_score_stock` 每只股票仅 `build_features` 一次，下发给 5 个 analyze_* 与 `_try_candidate`→`validate`，消灭 analysis→validator 的重复计算（每只股票原重算 6 次指标，现 1 次）。

未做（原评估已确认代码现状即如此）：超买合并（_st/_mo_is_overbought 已是 _is_overbought 别名）、一次性补丁配置化（_cap_short_term_by_sector 已由 SHORT_TERM_MAX_PER_SECTOR 驱动）。

测试修复（mock 目标随实现下沉而错位）：test_analysis.py 把 patch 目标由 scanner.analysis 改指 scanner.features，断言由 kwargs["period"] 改查 args[1]；test_validator.py 把 monkeypatch 的 lambda 补成兼容 (c, f=None)/(c, h, f=None) 的签名。

验证：重构后 pytest 全量 **423 passed**（与基线一致，分数语义未变）。

注：剩余 compute_* 调用均属固有（fallback 分支 / 顶背离偏移序列 RSI / 超买判定在"并入今日收盘价"序列上），非本次去重对象。

## Code Review：去重重构复审（15:44）

对 5 文件重构做通读复核（非仅看摘要）。结论：**无功能性回归，可安全合并**。

- 等价性逐条核对通过：orchestrator 单构建 feats（len>=5）下发给 5×analyze 与 validate；analyze 内部 historical_kline 与 orchestrator historical 同源（同 exclude-today），feats 复用等价；rebound 形态用全量 kline（含今日）、指标用 historical，与旧实现一致；_band_score 为 if/elif 阶梯的 1:1 收口，边界一致；build_features 的 kdj/adx/atr 用 [k.high]/[k.low]，与旧 _compute_*_indicators 同数据。
- 导入洁净：analysis.py 仅留 compute_ma + build_features；validator.py 4 个 compute_* 导入仍被 fallback/偏移计算真用，无死导入。
- 核心套件重跑 235 passed（全量基线 423）。

待办（非阻塞，不强制）：
1. P2：`_mo_divergence`（validator.py:227-228）feats 路径仍对 closes[:-1]/[:-2] 重算 RSI(6) 判背离——有意（feats 只存整段 rsi6，偏移序列无法复用），且旧实现也如此，未引入新开销；仅建议注释点明"故意偏移计算"。
2. P3：`_st_is_overbought`/`_mo_is_overbought`（validator.py:618-619）为 `_is_overbought` 别名，保留无害，可加一行注释说明，或直接调用原名。
3. P3：`build_features` 返回裸 dict，建议 TypedDict 以便 feats["rsi6"] 取值有类型检查；当前 feats.get("kdj") 与 feats["rsi6"] 混用是隐式契约。

后续（correctness 无碍，纯性能）：原评估"每只股票无条件跑 5 路打分、4/5 被丢弃"的算力浪费仍未动，如需进一步收敛可单独立项（按价格结构只跑命中策略）。
